const bstFromPreorder = (preorder) => {
  const bst = [preorder[0]]; // the root node will always be traversed first in a preorder
  // todo
  return bst;
};

module.exports = {
  bstFromPreorder,
};
